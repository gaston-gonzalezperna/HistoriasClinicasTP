
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<a asp-action="CrearRoles">Crear roles Basicos</a>

@if (ViewBag.Roles != null)
{
    <ul>
        @foreach (var rol in ViewBag.Roles)
        {
            <li>@rol</li>
        }
    </ul>
}


